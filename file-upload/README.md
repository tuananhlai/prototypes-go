# File upload via presigned URL

Demonstrate a fairly production-ready file upload flow, which uploads the file to a storage service then notifies the server on completion, instead of sending the file directly to the server.
